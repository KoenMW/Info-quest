export type Inputs =
  | "left_press"
  | "left_release"
  | "right_press"
  | "right_release"
  | "jump";

export type Data = {
  name: string;
  english: string;
  dutch: string;
  gained: boolean;
};
